List of things to do
====================

- Support GENERATED AS IDENTITY with later Oracle versions.

- Migrate triggers by defining a trigger function with the same name as
  the trigger.

- Add a boolean flag "migrate" for constraints and indexes.
